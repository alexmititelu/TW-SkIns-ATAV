
function initialize () {
    // var score =
    // var fileName = 
        if (fileName.includes("/") || fileName.includes("\\")){
        throw "Invalid file name.";
    }
    var path = "tabs/" + fileName;

}